# Example Environment Variables for Japanese Lexical Graph Project

# Gemini API Configuration
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_DEFAULT_MODEL=gemini-1.5-flash

# Wikidata Configuration
WIKIDATA_USER_AGENT=PythonWikidataLexicalGraph/1.0 (your-email@example.com)

# Database Configuration (for PostgreSQL)
# Replace with your actual database connection details
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DB_NAME

# Flask Secret Key (generate a strong random key)
# Used for session management, flash messages, etc.
# Example: openssl rand -hex 32
SECRET_KEY=your-strong-random-secret-key

# JWT (JSON Web Token) Configuration
# Used for token-based authentication (generate a strong random key)
# Example: openssl rand -hex 32
JWT_SECRET_KEY=your-strong-random-jwt-secret-key

# Flask Environment (optional, 'development' enables debug mode)
# FLASK_ENV=development
# FLASK_APP=app.py # Or your main application file e.g. run_app.py
FLASK_DEBUG=True # Enables debug mode for Flask development server
# Note: For production, ensure FLASK_ENV is set to 'production' and FLASK_DEBUG to False.
